class Solution {
public:
    bool isPerfectSquare(int n) {
        for (int i = 1; i <= sqrt(n); i++) { 
  
        // If (i * i = n) 
        if ((n % i == 0) && (n / i == i)) { 
            return true; 
        } 
    } 
    return false; 
    }
};
